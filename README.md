# 📝 美丽的待办事项

一个简洁高效的Flutter待办事项应用，支持跨平台部署。

## 🚀 快速开始

### Linux 平台部署

### 1. 安装依赖
```bash
# 进入项目目录
cd /path/to/todo

# 安装 Linux 构建依赖
./scripts/setup/install_linux_deps.sh
```

### 2. 构建 DEB 包
```bash
# 构建 DEB 包
./scripts/build/build_linux_deb.sh
```

### 3. 安装应用
```bash
# 安装生成的 DEB 包
sudo dpkg -i outputs/linux/todo-app-1.0.0-amd64.deb

# 如果有依赖问题
sudo apt-get install -f
```

## 📋 构建输出位置
- **DEB 包**: `outputs/linux/todo-app-1.0.0-amd64.deb`
- **应用安装位置**: `/opt/todo_app/`
- **启动命令**: `todo_app`

## 🌟 主要特性

- ✅ 简洁直观的用户界面
- 📱 跨平台支持 (Linux, macOS, Android)
- ⚡ 高性能的 Flutter 架构
- 🔒 本地数据存储，保护隐私
- 🎨 优雅的视觉设计

## 🏗️ 支持平台

| 平台 | 构建产物 | 状态 |
|------|----------|------|
| 🐧 Linux | DEB 包 | ✅ 完整支持 |
| 🍎 macOS | DMG 包 | ✅ 完整支持 |
| 🤖 Android | APK/AAB | ✅ 完整支持 |

## 📚 完整文档
- 部署指南: `docs/deployment/linux/BUILD_DEB_GUIDE.md`
- API 文档: `docs/api/`
- 脚本说明: `scripts/README.md`

## 🔧 手动构建步骤
如果脚本失败，请参考以下手动步骤：

1. 安装依赖: `sudo apt install libsecret-1-dev`
2. 构建应用: `cd src && flutter build linux --release`
3. 创建 DEB: 参考 `BUILD_DEB_GUIDE.md` 手动打包

## 🐛 常见问题
- **缺少依赖**: `sudo apt install libsecret-1-dev`
- **权限问题**: 确保 `.sh` 脚本有执行权限
- **依赖冲突**: `sudo apt-get install -f`

---
💡 提示：构建前请确保系统已安装 Flutter 和所有必需的开发工具。